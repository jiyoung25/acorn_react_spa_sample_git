import { BrowserRouter } from "react-router-dom";
import App from "./App_practice";

const Root = () => {
    return(
        <BrowserRouter>
            <App />
        </BrowserRouter>
    );
}

export default Root;